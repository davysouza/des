name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: create lib
      run: make lib
    - name: create tests
      run: make tests
    - name: create all
      run: make all
    - name: execute tests
      run: ./tests
